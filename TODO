(lb) Beware final sidtime minute etc.
    Actually very shrot and apparently no images in it
Currently finding 'sidtime' by rounding down....
(lb) Sort out them smudges
Deal with float BSCALE in a nicer way
Investigate whether the Milky Way is acting like a cloud
Check for negative fluxes
Clean up some of the early javascript variable names
Would be nice to know if gnuplots internal format counts leapseconds
Fix inconsistency of nameing of Line's fields
Stop using raw sql when it's not neccessary
Look for breaks in lines, and check if sextractor is wrong
Use postgres truncate
Zfill urls for neatness
Make generation_pk into a commandline argument
Speed up perline (multiprocessing, bulk_create)

Split out import points from matching them ?
Allow import of a useful small subset into the database for testing
